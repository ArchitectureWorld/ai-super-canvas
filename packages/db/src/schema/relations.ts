// Drizzle table definitions and database-enforced foreign keys are the S1 contract.
// Query relation helpers are added alongside repositories in Task 4 to avoid
// creating a second, partially implemented navigation API here.
export const controlPlaneRelationsVersion = 1 as const;
